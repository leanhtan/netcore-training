@model string
@using SimpleBlogEngine.Models

<form asp-action="DeleteCategory" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Delete Category" })
    <div class="modal-body form-horizontal">
        Are you want to delete @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>